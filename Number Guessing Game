let randomNumber = Math.floor(Math.random() * 10) + 1;
let guess = 5; // change this number to guess

if (guess === randomNumber) {
    console.log("Correct Guess! 🎉");
} else {
    console.log("Wrong Guess! The number was " + randomNumber);
}
